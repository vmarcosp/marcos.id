@react.component
let make = () => {
  <section>
    <div>
      <h1> {"Build. Ship."->s} </h1>
      <p>
        {"Projects, experiments, and open-source contributions I've had the privilege to create or maintain."->s}
      </p>
    </div>
  </section>
}
